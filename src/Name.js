import React from 'react';

const styles = {
  root: {
    textAlign: 'center',
    fontFamily: 'Poiret One',
    marginTop: '20px',
  },
};

const Name = () => (
  <div style={styles.root}>
    <h1> Michael Stuart </h1>
  </div>
);

export default Name;
